# OpenMAIC CLAUDE.md

**Status:** ⚠️ ARCHIVED 2026-05-05 (Phase B.4 complete). Not built, not deployed.

Classroom + course UIs are in `web/app/classroom/` and `web/app/course/`. Generation pipeline + prompts + integrations are in `web/lib/`. See project root `CLAUDE.md` for everything current — this tree is preserved for historical reference only.

If you need the last working OpenMAIC commit before retirement, browse git history around 2026-05-05 (the B.4 sweep). The container, the `bind tailscale/openmaic` Caddyfile block, and the `openmaic-data` volume are all gone from Pironman.

Hook false-positive workarounds that used to live here are documented in the project root CLAUDE.md under "Hook false positives when writing TypeScript".
