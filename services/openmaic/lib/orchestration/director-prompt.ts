/**
 * Stub — real implementation moved to web/lib/orchestration/director-prompt.ts (Phase B.1).
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function buildDirectorPrompt(..._args: any[]): string {
  throw new Error('buildDirectorPrompt: moved to web/ in Phase B.1');
}
