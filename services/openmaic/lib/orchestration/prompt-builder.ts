/**
 * Stub — real implementation moved to web/lib/orchestration/prompt-builder.ts (Phase B.1).
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function buildStructuredPrompt(..._args: any[]): string {
  throw new Error('buildStructuredPrompt: moved to web/ in Phase B.1');
}

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function buildAgentSystemPrompt(..._args: any[]): string {
  throw new Error('buildAgentSystemPrompt: moved to web/ in Phase B.1');
}
