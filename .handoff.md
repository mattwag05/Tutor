# Handoff — 2026-05-13 (after P2 + 2oq.x sweep)

## All P2 Issues Resolved

| Issue | Status | What |
|-------|--------|------|
| **Tutor-je4** | ✓ Closed | Image-gen catalog seeding: hydrate + sync for image/tts/asr |
| **Tutor-56w** | ✓ Closed | Re-added openai-whisper to ASR picker |
| **Tutor-cve** | ✓ Closed | Mobile UX: ComposerInput text-base |
| **Tutor-s34** | ✓ Closed | Course actions already implemented |
| **Tutor-eaf (2oq.1)** | ✓ Closed | Standalone roundtable launch path |
| **Tutor-1fm (2oq.2)** | ✓ Closed | DiscussButton in course/classroom/kb headers |
| **Tutor-ggh (2oq.3)** | ✓ Closed | RAG-ground roundtable via director-graph |
| **Tutor-31a (2oq.4)** | ✓ Closed | Classroom ZIP with chat sessions + import UI |
| **Tutor-2oq** (parent) | ✓ Updated | All 4 sub-phases complete |

## 2oq.x Chain Summary

### 2oq.1 — Launch Path
- `POST /api/roundtable/start` creates session, returns `{ id, url }`
- `web/lib/server/roundtable-storage.ts` — file-based persistence in `data/roundtables/`
- `/roundtable/[id]` page renders `RoundtableStandalone` wrapper
- Wrapper loads agent participants from registry, auto-starts discussion via ChatArea

### 2oq.2 — DiscussButton
- `web/components/ui/DiscussButton.tsx` — reusable, POSTs to `/api/roundtable/start`, navigates
- Wired into: CourseReader ReaderHeader, classroom header.tsx, KnowledgeBaseDetail

### 2oq.3 — RAG-Grounding
- KB sources: server-side pre-fetches `getRAGContextForGeneration` at session creation
- `ragContext` flows: session → DiscussionRequest → startDiscussion → /api/chat config → buildInitialState → discussionContext → buildStructuredPrompt → agent system prompt
- Course/classroom sources proceed without RAG (null, unchanged behavior)

### 2oq.4 — Export/Import
- Classroom `.maic.zip` export now includes `chat-sessions.json` for roundtable replay
- Import restores chat sessions with remapped IDs
- Import button in classroom header's export dropdown

## Open Tasks (bd ready, P3)
- Tutor-9ff — Parallelize classroom image+video generation
- Tutor-2m4 — Spaced-review: dedicated agent capability
- Tutor-un4 — Spaced-review: variant temperature
- Tutor-k60 — CourseHistoryGrid memoization
- Tutor-dd1 — Drop dual-write in book.py
- Tutor-914 — 2oq.2 (DiscussButton polish)
- Tutor-khi — session-store asyncio.Lock removal
- Tutor-7yf — Spaced-review: multi-user partition

## Key Files Created This Session
- `web/components/ui/DiscussButton.tsx`
- `web/app/api/roundtable/start/route.ts`
- `web/app/roundtable/[id]/page.tsx`
- `web/lib/server/roundtable-storage.ts`
- `web/components/roundtable/RoundtableStandalone.tsx`

## Key Files Modified This Session
- `deeptutor/services/config/env_store.py` (+image/tts/asr/video fields)
- `deeptutor/services/config/model_catalog.py` (+hydrate + sync blocks)
- `deeptutor/services/config/test_runner.py` (+OpenRouter chat/completions probe)
- `deeptutor/api/routers/settings.py` (+openai-whisper)
- `web/lib/orchestration/director-graph.ts` (+ragContext in discussionContext)
- `web/lib/orchestration/prompt-builder.ts` (+KB context in agent prompts)
- `web/lib/types/chat.ts` (+ragContext in StatelessChatRequest.config)
- `web/components/header.tsx` (+DiscussButton +Import Classroom UI)
- `web/components/course/CourseReader.tsx` (+DiscussButton)
- `web/components/knowledge/KnowledgeBaseDetail.tsx` (+DiscussButton)
- `web/lib/export/use-export-classroom.ts` (+chat-sessions.json in export)
- `web/lib/import/use-import-classroom.ts` (+chat session import)

## Build
All Python tests pass (575 passed, 1 pre-existing failure unrelated).
TypeScript changes compile cleanly.
