# Handoff — 2026-05-13 (session 3-5)

## All P2 Items Resolved

| Issue | Status | What |
|-------|--------|------|
| **DeepTutor-je4** | ✓ Closed | Image-gen catalog seeding: hydrate + sync for image/tts/asr |
| **DeepTutor-56w** | ✓ Closed | Re-added openai-whisper to ASR picker |
| **DeepTutor-cve** | ✓ Closed | Mobile UX: ComposerInput text-base |
| **DeepTutor-s34** | ✓ Closed | Course actions already implemented |
| **DeepTutor-eaf (2oq.1)** | ✓ Closed | Standalone roundtable launch path |
| **DeepTutor-1fm (2oq.2)** | ✓ Closed | DiscussButton in course/classroom/kb headers |
| **DeepTutor-ggh (2oq.3)** | ✓ Closed | RAG-ground roundtable via director-graph |
| **DeepTutor-31a (2oq.4)** | ○ P3 | Classroom export ZIP for replay (remaining) |

## 2oq.x Chain Complete (launch + button + RAG)

**2oq.1 — Launch path:** `POST /api/roundtable/start` + `/roundtable/[id]` page + `RoundtableStandalone` wrapper

**2oq.2 — DiscussButton:** Reusable button wired into course reader header, classroom viewer header, and KB detail header

**2oq.3 — RAG-grounding:** Full chain:
- KB source: server-side pre-fetches `getRAGContextForGeneration` at session creation
- Passed through `DiscussionRequest` → `startDiscussion` → `/api/chat` config → `buildInitialState` → `discussionContext.ragContext`
- Injected into agent system prompts via `buildDiscussionContextSection` as "# Knowledge Base Context"

## Remaining P3 Items
- DeepTutor-31a — 2oq.4: Classroom export ZIP
- DeepTutor-914 — 2oq.2 (DiscussButton, now just UI polish)
- DeepTutor-2oq — Parent issue, partial (launch + button + RAG done, export pending)
- DeepTutor-9ff — Parallelize classroom image+video generation
- DeepTutor-2m4, DeepTutor-un4, DeepTutor-k60, etc.

## Key Files
- All 2oq.x files listed in prior handoff entries
- `web/lib/orchestration/director-graph.ts` — discussionContext extended with ragContext
- `web/lib/orchestration/prompt-builder.ts` — RAG KB context injected into agent prompts
- `web/lib/types/chat.ts` — ragContext in StatelessChatRequest.config
- `web/app/api/roundtable/start/route.ts` — KB pre-fetch
- `web/components/roundtable/index.tsx` — DiscussionRequest extended

## Next Step
2oq.4 (classroom export ZIP) is the last phase of the 2oq parent, or pick another P3 issue.
